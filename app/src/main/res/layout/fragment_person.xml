<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:gravity="center"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <!-- TODO: Update blank fragment layout -->
    <Button
        android:id="@+id/btnThongtincanhan"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="50dp"
        android:gravity="left"
        android:text="Thông tin cá nhân" />

    <Button
        android:id="@+id/Doimatkhau"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/btnThongtincanhan"
        android:layout_marginTop="-59dp"
        android:gravity="left"
        android:text="Đổi mật khẩu "
        android:textSize="15dp" />

    <Button
        android:id="@+id/qlthucung"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/Doimatkhau"
        android:layout_marginTop="45dp"
        android:gravity="left"
        android:text="Quản lý thú cưng "/>

    <Button
        android:id="@+id/btnDangXuat"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/qlthucung"
        android:layout_marginTop="50dp"
        android:layout_marginBottom="-90dp"
        android:backgroundTint="#333366"
        android:text="Đăng xuất"
        android:textColor="#fff"
        android:textSize="16dp"
        android:textStyle="bold" />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/imgavt"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="-100dp"
        android:clickable="true"
        tools:srcCompat="@tools:sample/avatars" />

</RelativeLayout>